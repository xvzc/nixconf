name: nvim
root: ~/.config/nvim

pre_window: export NVIM_APPNAME=nvim && tmux setenv NVIM_APPNAME $NVIM_APPNAME
windows:
  - editor:
      layout: main-vertical
      panes:
        - editor:
          - nvim
  - shell:
      layout: main-vertical
